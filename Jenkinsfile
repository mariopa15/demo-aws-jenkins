pipeline {    agent any    environment {        AWS_REGION = 'us-east-1'        BUCKET_NAME = 'mi-bucket-unico-jenkins-2025' // Cambia por un nombre único    }    stages {        stage('Crear S3 Bucket') {            steps {                echo "☁️ Creando bucket S3 con Terraform"                sh '''                    # Crear archivo Terraform                    cat > s3.tf <<EOLterraform {  required_providers {    aws = {      source  = "hashicorp/aws"      version = "~> 5.0"    }  }}provider "aws" {  region = "${AWS_REGION}"}resource "aws_s3_bucket" "mi_bucket" {  bucket = "${BUCKET_NAME}"  acl    = "private"}EOL                    # Inicializar Terraform                    terraform init -input=false -no-color                    # Aplicar configuración automáticamente                    terraform apply -input=false -auto-approve                '''            }        }    }    post {        always {            echo "📜 Pipeline finalizado. Revisa los logs."        }        success {            echo "✅ Bucket creado correctamente"        }        failure {            echo "❌ Falló la creación del bucket. Revisa errores anteriores"        }    }}